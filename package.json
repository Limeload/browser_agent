{
  "name": "voice-enabled-browser-agent",
  "version": "1.0.0",
  "description": "Voice-enabled browser automation with AI intent parsing and monitoring",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:executor\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --port 8000",
    "dev:executor": "cd executor && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../executor && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm run build",
    "start": "concurrently \"npm run start:backend\" \"npm run start:executor\"",
    "start:backend": "cd backend && python -m uvicorn main:app --port 8000",
    "start:executor": "cd executor && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "frontend",
    "executor"
  ]
}
